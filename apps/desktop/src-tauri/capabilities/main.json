{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the main webview can retrieve its locally provisioned session token from the process environment. No shell, filesystem, HTTP plugin or shortcut management permission.",
  "windows": ["main"],
  "permissions": ["allow-local-session-token"]
}
