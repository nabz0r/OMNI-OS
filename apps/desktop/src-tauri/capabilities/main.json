{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Only the main webview can start its encrypted local core, make authorized core requests, export bounded JSON/CSV metadata, and open the fixed official setup resource. Device keys remain native. No arbitrary shell, filesystem, HTTP plugin or shortcut management permission.",
  "windows": ["main"],
  "permissions": [
    "allow-native-session",
    "allow-core-request",
    "allow-save-metadata",
    "allow-open-setup-resource"
  ]
}
