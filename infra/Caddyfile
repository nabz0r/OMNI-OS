{$OMNI_PUBLIC_HOST} {
    encode zstd gzip
    header {
        Strict-Transport-Security "max-age=31536000"
        X-Content-Type-Options "nosniff"
        Referrer-Policy "no-referrer"
    }
    # No HTTP access logging: payload validation errors must not echo input.
    reverse_proxy collector:3008
}
